NAME

      gcloud preview replica-pools replicas - manage replicas in a replica pool

SYNOPSIS

      gcloud preview replica-pools replicas COMMAND
         [--api-version API_VERSION; default="v1beta1"] [--format FORMAT]
         [--help] --pool POOL [--project PROJECT] [--quiet, -q] [-h]

DESCRIPTION

      Manage replicas in a replica pool.

FLAGS

      --pool POOL

         Replica pool Name

GLOBAL FLAGS

      --api-version API_VERSION; default="v1beta1"

         Optional API version.

      --format FORMAT

         Specify a format for printed output. By default, a command-specific
         human-friendly output format is used. Setting this flag to one of the
         available options will serialize the result of the command in the
         chosen format and print it to stdout. Supported formats are: json,
         text, yaml.

      --help

         Display detailed help.

      --project PROJECT

         The Google Cloud Platform project name to use for this invocation. If
         omitted then the current project is assumed.

      --quiet, -q

         Disable all interactive prompts when running gcloud commands. If input
         is required, defaults will be used, or an error will be raised.

      -h

         Print a summary help and exit.

COMMANDS

      COMMAND is one of the following:

      delete

         Deletes a single replica from a replica pool.

      get

         Gets detailed information about a single replica in a replica pool.

      list

         List all replicas in a replica pool.

      restart

         Restart a single replica in a replica pool.

NOTES

      This command is in the Google Cloud SDK preview component. See installing
      components if it is not installed.

